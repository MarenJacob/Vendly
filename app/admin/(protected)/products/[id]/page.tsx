import { redirect, notFound } from 'next/navigation'; import Link from 'next/link'; import { getAdminSession } from '@/lib/admin-auth'; import { prisma } from '@/lib/prisma'; import ProductForm from '@/components/admin/ProductForm';
export default async function EditProduct({params}:{params:Promise<{id:string}>}){if(!await getAdminSession())redirect('/admin/login');const {id}=await params;const product=await prisma.product.findUnique({where:{id},include:{images:true,videos:true}});if(!product)notFound();return <main className="min-h-screen bg-slate-50 px-4 py-8"><div className="mx-auto max-w-4xl"><Link href="/admin/products" className="text-sm underline">← Products</Link><h1 className="mt-5 text-3xl font-semibold">Edit product</h1><p className="mt-2 mb-7 text-slate-500">Update catalogue details, inventory and listing media.</p><ProductForm initial={JSON.parse(JSON.stringify(product,(k,v)=>typeof v==='object'&&v?.constructor?.name==='Decimal'?v.toString():v))}/></div></main>}

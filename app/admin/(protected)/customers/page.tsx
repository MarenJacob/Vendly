'use client'; import {useEffect,useState} from 'react'; import {Search, Users, Mail, Phone} from 'lucide-react';
export default function Customers(){const [items,setItems]=useState<any[]>([]);const [q,setQ]=useState('');const [loading,setLoading]=useState(true);useEffect(()=>{fetch('/api/admin/customers').then(r=>r.json()).then(d=>setItems(d.customers||[])).finally(()=>setLoading(false))},[]);const filtered=items.filter(x=>`${x.name||''} ${x.email} ${x.phone||''}`.toLowerCase().includes(q.toLowerCase()));return <><div><p className="eyebrow text-slate-500">Customers</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Customer directory</h1><p className="mt-2 text-sm text-slate-500">Understand who is buying and keep customer information accessible.</p></div><div className="mt-7 flex max-w-md items-center gap-3 rounded-xl border border-slate-200 bg-white px-3"><Search size={17} className="text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search customers…" className="w-full bg-transparent py-3 text-sm outline-none"/></div><div className="mt-5 overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-b bg-slate-50 text-xs uppercase tracking-wider text-slate-400"><tr><th className="px-5 py-4">Customer</th><th>Contact</th><th>Orders</th><th>Messages</th><th>Joined</th></tr></thead><tbody>{filtered.map(c=><tr key={c.id} className="border-b last:border-0 hover:bg-slate-50"><td className="px-5 py-4"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-full bg-slate-100 font-semibold">{(c.name||c.email).slice(0,1).toUpperCase()}</span><div><p className="font-semibold">{c.name||'Unnamed customer'}</p><p className="text-xs text-slate-400">{c.email}</p></div></div></td><td><div className="space-y-1 text-xs text-slate-500"><p className="flex gap-2"><Mail size={13}/>{c.email}</p>{c.phone&&<p className="flex gap-2"><Phone size={13}/>{c.phone}</p>}</div></td><td className="font-medium">{c._count.orders}</td><td>{c._count.messages}</td><td className="text-slate-500">{new Date(c.createdAt).toLocaleDateString()}</td></tr>)}{!loading&&!filtered.length&&<tr><td colSpan={5} className="p-14 text-center text-slate-400"><Users className="mx-auto mb-3"/>No customers found.</td></tr>}</tbody></table></div></div></>}

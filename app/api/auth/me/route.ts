import { NextResponse } from 'next/server'; import { getCustomer } from '@/lib/customer-auth'; export async function GET(){const user=await getCustomer(); if(!user)return NextResponse.json({user:null}); return NextResponse.json({user:{id:user.id,name:user.name,email:user.email,phone:user.phone}});}

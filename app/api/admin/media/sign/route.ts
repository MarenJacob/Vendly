import {NextResponse} from 'next/server'; import {getAdminSession} from '@/lib/admin-auth'; import {cloudinarySignature} from '@/lib/cloudinary'; export async function POST(){if(!await getAdminSession())return NextResponse.json({error:'Unauthorized'},{status:401});const cloudName=process.env.CLOUDINARY_CLOUD_NAME,apiKey=process.env.CLOUDINARY_API_KEY;if(!cloudName||!apiKey)return NextResponse.json({error:'Cloudinary is not configured.'},{status:503});const timestamp=Math.floor(Date.now()/1000),folder='vendly/products',signature=cloudinarySignature({folder,timestamp});return NextResponse.json({cloudName,apiKey,timestamp,folder,signature});}


'use client'; import {useEffect,useState} from 'react'; import Link from 'next/link'; import {ArrowLeft,ArrowUpRight,Package} from 'lucide-react'; import {formatNaira} from '@/lib/products';
export default function Orders(){const [orders,setOrders]=useState<any[]>([]);const [state,setState]=useState('loading');useEffect(()=>{fetch('/api/account/orders').then(async r=>{if(r.status===401){setState('guest');return}const j=await r.json();setOrders(j.orders||[]);setState('ready')})},[]);if(state==='loading')return <div className="container py-24"><div className="skeleton h-10 w-52"/></div>;if(state==='guest')return <div className="container py-24 text-center"><Package className="mx-auto"/><h1 className="mt-5 text-4xl font-bold">Sign in to see your orders.</h1><Link href="/account/login" className="btn-primary mt-7 inline-flex">Sign in</Link></div>;return <div className="container py-12 md:py-20"><Link href="/account" className="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[.14em] text-[rgba(0,0,0,0.4)]"><ArrowLeft size={14}/> Account</Link><h1 className="mt-8 text-5xl font-bold tracking-[-.07em]">Your orders.</h1>{orders.length===0?<div className="mt-12 rounded-3xl border border-dashed border-[rgba(0,0,0,0.15)] p-14 text-center"><p className="text-[rgba(0,0,0,0.5)]">No orders yet.</p><Link href="/shop" className="mt-5 inline-flex items-center gap-2 font-bold">Start shopping <ArrowUpRight size={15}/></Link></div>:<div className="mt-10 grid gap-3">{orders.map(o=><Link key={o.id} href={`/track?reference=${encodeURIComponent(o.reference)}`} className="rounded-3xl border border-[rgba(0,0,0,0.1)] bg-white p-6 transition hover:-translate-y-0.5"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="font-mono text-xs text-[rgba(0,0,0,0.4)]">{o.reference}</p><p className="mt-2 text-sm font-bold">{o.status}</p></div><div className="text-right"><p className="font-semibold">{formatNaira(Number(o.total))}</p><p className="mt-1 text-xs text-[rgba(0,0,0,0.4)]">{new Date(o.createdAt).toLocaleDateString('en-NG')}</p></div></div><div className="mt-5 flex items-center justify-between border-t border-[rgba(0,0,0,0.1)] pt-4 text-xs text-[rgba(0,0,0,0.45)]"><span>{o.items.length} item{o.items.length===1?'':'s'}</span><span className="font-bold text-black">Track order <ArrowUpRight className="ml-1 inline" size={13}/></span></div></Link>)}</div>}</div>}

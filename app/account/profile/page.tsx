'use client';
import {useEffect,useState} from 'react';
import Link from 'next/link';
import {ArrowLeft,Check,UserRound} from 'lucide-react';
export default function Profile(){const [user,setUser]=useState<any>(null);const [name,setName]=useState('');const [phone,setPhone]=useState('');const [status,setStatus]=useState('');useEffect(()=>{fetch('/api/auth/me').then(r=>r.json()).then(d=>{if(d.user){setUser(d.user);setName(d.user.name||'');setPhone(d.user.phone||'')}})},[]);async function save(e:any){e.preventDefault();setStatus('Saving…');const r=await fetch('/api/account/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,phone})});const d=await r.json();setStatus(r.ok?'Profile updated.':d.error||'Could not save.');if(r.ok)setUser(d.user)}if(!user)return <div className="container py-24 text-center text-sm text-[rgba(0,0,0,0.5)]">Loading account…</div>;return <div className="container max-w-3xl py-12 md:py-20"><Link href="/account" className="inline-flex items-center gap-2 text-sm font-semibold"><ArrowLeft size={15}/> Account</Link><div className="mt-8 rounded-[2rem] border border-[rgba(0,0,0,0.1)] bg-white p-7 shadow-sm md:p-10"><div className="flex items-center gap-4"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-black text-white"><UserRound size={20}/></span><div><p className="eyebrow text-[rgba(0,0,0,0.4)]">Personal details</p><h1 className="mt-1 text-3xl font-bold tracking-[-.05em]">Your profile</h1></div></div><form onSubmit={save} className="mt-9 space-y-5"><label className="block text-sm font-semibold">Full name<input className="input mt-2" value={name} onChange={e=>setName(e.target.value)} required/></label><label className="block text-sm font-semibold">Email<input className="input mt-2 opacity-60" value={user.email} readOnly/></label><label className="block text-sm font-semibold">Phone<input className="input mt-2" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="080…"/></label><div className="flex flex-col gap-3 border-t pt-5 sm:flex-row sm:items-center sm:justify-between"><span className="text-sm text-[rgba(0,0,0,0.5)]">{status}</span><button className="btn-primary"><Check size={16}/> Save changes</button></div></form></div></div>}

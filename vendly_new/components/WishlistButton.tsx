'use client';import {Heart} from 'lucide-react';import {useWishlist} from './WishlistProvider';export default function WishlistButton({productId}:{productId:string}){const {has,toggle}=useWishlist();const liked=has(productId);return <button onClick={()=>toggle(productId)} aria-label={liked?'Remove from wishlist':'Add to wishlist'} className={`grid h-11 w-11 shrink-0 place-items-center rounded-full border transition ${liked?'border-black bg-black text-white':'border-[rgba(0,0,0,0.1)] bg-white hover:border-black'}`}><Heart size={18} fill={liked?'currentColor':'none'}/></button>}

'use client';
import {useEffect,useState} from 'react';
import {History, ShieldCheck} from 'lucide-react';
export default function AuditPage(){const [logs,setLogs]=useState<any[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{fetch('/api/admin/audit').then(r=>r.json()).then(d=>setLogs(d.logs||[])).finally(()=>setLoading(false))},[]);return <div><div><p className="eyebrow text-slate-500">Governance</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Activity log</h1><p className="mt-2 text-sm text-slate-500">A transparent record of important administrative actions.</p></div><div className="mt-7 overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="flex items-center gap-3 border-b px-5 py-4"><span className="grid h-9 w-9 place-items-center rounded-xl bg-slate-100"><ShieldCheck size={17}/></span><div><h2 className="text-sm font-semibold">Recent activity</h2><p className="text-xs text-slate-400">Latest 100 events</p></div></div>{loading?<div className="p-12 text-center text-sm text-slate-400">Loading activity…</div>:logs.length?<div className="divide-y">{logs.map(x=><div key={x.id} className="flex items-start gap-4 px-5 py-4"><span className="mt-0.5 grid h-9 w-9 shrink-0 place-items-center rounded-xl bg-slate-50"><History size={16}/></span><div className="min-w-0 flex-1"><p className="text-sm font-semibold">{x.action.replaceAll('_',' ')}</p><p className="mt-1 text-xs text-slate-500">{x.entity}{x.details?' · '+x.details:''}</p></div><time className="shrink-0 text-xs text-slate-400">{new Date(x.createdAt).toLocaleString()}</time></div>)}</div>:<div className="p-12 text-center text-sm text-slate-400">No activity recorded yet.</div>}</div></div>}

import { redirect } from 'next/navigation'; import Link from 'next/link'; import { getAdminSession } from '@/lib/admin-auth'; import ProductForm from '@/components/admin/ProductForm';
export default async function NewProduct(){if(!await getAdminSession())redirect('/admin/login');return <main className="min-h-screen bg-slate-50 px-4 py-8"><div className="mx-auto max-w-4xl"><Link href="/admin/products" className="text-sm underline">← Products</Link><h1 className="mt-5 text-3xl font-semibold">Add product</h1><p className="mt-2 mb-7 text-slate-500">Create a catalogue item with an optional category-listing video.</p><ProductForm/></div></main>}

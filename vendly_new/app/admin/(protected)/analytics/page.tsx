'use client'; import {useEffect,useState} from 'react'; import {BarChart3, CircleDollarSign, Package, ShoppingBag, Users, Download, Trophy} from 'lucide-react';
export default function Analytics(){const [d,setD]=useState<any>();useEffect(()=>{fetch('/api/admin/analytics').then(r=>r.json()).then(setD)},[]);const statuses=d?.byStatus||[];return <><div><p className="eyebrow text-slate-500">Performance</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Store analytics</h1><p className="mt-2 text-sm text-slate-500">A lightweight operational view of your catalogue and sales.</p></div><div className="mt-7 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Revenue',`₦${(d?.revenue||0).toLocaleString()}`,CircleDollarSign],['Orders',d?.orders||0,ShoppingBag],['Customers',d?.customers||0,Users],['Products',d?.products||0,Package]].map(([a,b,I])=><div className="rounded-2xl border border-slate-200 bg-white p-5" key={a as string}><I size={19}/><p className="mt-6 text-xs uppercase tracking-wider text-slate-400">{a}</p><p className="mt-1 text-2xl font-semibold">{b}</p></div>)}</div><div className="mt-5 flex justify-end"><a href="/api/admin/reports/export" className="btn-secondary"><Download size={15}/> Export orders CSV</a></div><section className="mt-6 grid gap-6 lg:grid-cols-2"><div className="rounded-2xl border border-slate-200 bg-white p-6"><div className="flex items-center gap-3"><BarChart3 size={19}/><h2 className="font-semibold">Order status</h2></div><div className="mt-6 space-y-4">{statuses.map((x:any)=><div key={x.status}><div className="flex justify-between text-sm"><span className="font-medium">{x.status}</span><span className="text-slate-400">{x._count._all} orders · ₦{Number(x._sum.total||0).toLocaleString()}</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-slate-900" style={{width:`${Math.max(4,Math.round((x._count._all/Math.max(d?.orders||1,1))*100))}%`}}/></div></div>)}{!statuses.length&&<p className="py-10 text-center text-sm text-slate-400">Sales data will appear here as orders arrive.</p>}</div></div><div className="rounded-2xl bg-slate-950 p-6 text-white"><p className="text-xs font-bold uppercase tracking-[.16em] text-slate-500">Operator note</p><h2 className="mt-4 text-2xl font-semibold tracking-tight">Keep the catalogue fresh.</h2><p className="mt-3 max-w-md text-sm leading-6 text-slate-400">Use category-listing videos selectively for products where seeing the item in motion adds confidence. Keep product imagery strong and consistent across the store.</p></div></section><section className="mt-6 rounded-2xl border border-slate-200 bg-white p-6"><div className="flex items-center gap-3"><Trophy size={18}/><h2 className="font-semibold">Top products · last 30 days</h2></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{(d?.topProducts||[]).map((x:any)=><div key={x.productId} className="rounded-2xl bg-slate-50 p-4"><p className="truncate text-sm font-semibold">{x.product?.name||'Product'}</p><p className="mt-2 text-xs text-slate-400">{x._sum.quantity||0} units</p></div>)}{!(d?.topProducts||[]).length&&<p className="text-sm text-slate-400">Top products will appear as completed sales arrive.</p>}</div></section></>}

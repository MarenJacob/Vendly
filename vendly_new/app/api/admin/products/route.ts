import {NextResponse} from 'next/server';
import { prisma } from '@/lib/prisma';
import {getAdminSession} from '@/lib/admin-auth';
import {cleanText} from '@/lib/validation';
export async function GET(){if(!await getAdminSession())return NextResponse.json({error:'Unauthorized'},{status:401});const products=await prisma.product.findMany({include:{category:true,images:true,videos:true},orderBy:{updatedAt:'desc'}});return NextResponse.json({products});}
export async function POST(req:Request){const actor=await getAdminSession();if(!actor)return NextResponse.json({error:'Unauthorized'},{status:401});try{const b=await req.json();const name=cleanText(b.name,160),slug=cleanText(b.slug,180).toLowerCase(),description=cleanText(b.description,3000),price=Number(b.price),stock=Math.floor(Number(b.stock||0)),categoryId=cleanText(b.categoryId,80),imageUrls=(Array.isArray(b.imageUrls)?b.imageUrls:[]).map((u:string)=>cleanText(u,1000)).filter(Boolean).slice(0,10),videoUrl=cleanText(b.videoUrl,1000);if(name.length<2||!slug||!Number.isFinite(price)||price<0||!categoryId||!Number.isFinite(stock)||stock<0)return NextResponse.json({error:'Name, slug, category, price and valid stock are required.'},{status:400});const product=await prisma.$transaction(async tx=>{const created=await tx.product.create({data:{name,slug,description,price,oldPrice:b.oldPrice?Number(b.oldPrice):null,stock,isPreorder:Boolean(b.isPreorder),preorderNote:b.isPreorder?cleanText(b.preorderNote,140)||null:null,featured:Boolean(b.featured),categoryId,images:imageUrls.length?{create:imageUrls.map((url:string,i:number)=>({url,alt:name,sortOrder:i}))}:undefined,videos:videoUrl?{create:{url:videoUrl,posterUrl:imageUrls[0]||null}}:undefined}});await tx.auditLog.create({data:{actor,action:'PRODUCT_CREATED',entity:'Product',entityId:created.id,details:created.name}});return created;});return NextResponse.json({product},{status:201});}catch(e:any){return NextResponse.json({error:e?.code==='P2002'?'That slug is already in use.':'Could not create product.'},{status:400});}}

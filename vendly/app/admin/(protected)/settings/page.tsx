import { ShieldCheck, CreditCard, Cloud, Globe2 } from 'lucide-react';
export default function Settings(){const checks=[['Database','DATABASE_URL','Required for live catalogue, orders and customers.',Globe2],['Payments','PAYSTACK_SECRET_KEY','Server-side key. Never expose this in client code.',CreditCard],['Media','CLOUDINARY_*','Connect Cloudinary when real product media is ready.',Cloud],['Admin security','ADMIN_SESSION_SECRET','Use a long random secret before production.',ShieldCheck]] as const;return <div><p className="eyebrow">Settings</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Production readiness.</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Vendly keeps sensitive configuration in environment variables. This page gives the team a clean checklist without exposing secrets.</p><div className="mt-8 grid gap-4 lg:grid-cols-2">{checks.map(([title,key,desc,Icon])=><div key={key} className="rounded-3xl border border-slate-200 bg-white p-6"><div className="flex items-start justify-between"><div className="grid h-11 w-11 place-items-center rounded-2xl bg-slate-950 text-white"><Icon className="h-5 w-5"/></div><span className="rounded-full bg-slate-100 px-3 py-1 text-[10px] font-bold uppercase tracking-widest text-slate-500">Environment</span></div><h2 className="mt-6 font-semibold">{title}</h2><code className="mt-2 block text-xs text-slate-500">{key}</code><p className="mt-3 text-sm leading-6 text-slate-500">{desc}</p></div>)}</div><div className="mt-8 rounded-3xl bg-slate-950 p-6 text-white"><p className="text-xs font-bold uppercase tracking-[.18em] text-slate-400">Launch rule</p><p className="mt-3 max-w-2xl text-sm leading-7 text-slate-300">Use HTTPS, live Paystack keys, a strong admin password/session secret, a production PostgreSQL database, real Cloudinary credentials and a real support number before public launch.</p></div><div className="mt-4 rounded-3xl border border-[rgba(0,0,0,0.1)] bg-white p-6"><p className="text-sm font-bold">Cloudinary media</p><p className="mt-2 text-xs leading-6 text-[rgba(0,0,0,0.5)]">Phase 6 adds signed uploads for product images and videos. Keep API secrets server-side; only the signed upload payload reaches the browser.</p></div></div>}
